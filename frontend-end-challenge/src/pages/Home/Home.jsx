import React from 'react';
import { PostCard, Button } from '../../components';

export default function Home() {
  return (
    <section>
      <div>{}</div>
      <div>
        <Button value="Carregar Mais" />
      </div>
    </section>
  );
}
