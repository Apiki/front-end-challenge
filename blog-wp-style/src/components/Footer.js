import React from 'react';

const Footer = (props) => {
  const { styles } = props;
  return (
    <div className={`${styles}`}><h3>Site desenvolvido por Marco Barbosa</h3></div>
  );
};

export default Footer;
