// Dependencies
import styled from "styled-components";

// Main
export const Container = styled.button`
    position: fixed;
    right: 1rem;
    bottom: 1.75rem;
    z-index: 10;
    width: 3rem;
    height: 3rem;

    .icon {
        width: max-content;
        height: max-content;
        margin: auto;
        display: block;
    }
`