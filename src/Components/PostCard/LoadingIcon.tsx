import { CircleNotch } from "phosphor-react";

export default function LoadingIcon() {
    return (
        <div>
            <CircleNotch size={32} className="animate-spin text-center self-center justify-center m-auto" />
        </div>
    )
}